<%- contentFor('body') %>

<div class="main-content">
    <div class="docs-header bg-gradient-to-r from-blue-600 via-indigo-600 to-purple-600 text-white p-10 rounded-2xl mb-10 shadow-xl relative overflow-hidden">
        <div class="absolute -top-24 -right-24 w-64 h-64 bg-white opacity-10 rounded-full"></div>
        <div class="absolute -bottom-32 -left-32 w-96 h-96 bg-white opacity-10 rounded-full"></div>
        <div class="relative z-10">
            <h1 class="text-5xl font-bold mb-4 animate-fade-in"><%= locale.docs.title %></h1>
            <p class="text-xl opacity-90 max-w-2xl animate-fade-in-delay"><%= locale.docs.subtitle %></p>
        </div>
    </div>

    <div class="bg-gray-800 rounded-xl p-6 mb-10 shadow-lg sticky top-4 z-20 animate-slide-in" id="toc">
        <h2 class="text-2xl font-bold text-white mb-4 flex items-center">
            <i class="fas fa-list-ul text-blue-400 mr-3"></i>
            Quick Navigation
        </h2>
        <div class="grid grid-cols-1 md:grid-cols-3 lg:grid-cols-5 gap-4">
            <a href="#getting-started" class="p-3 bg-gray-700 rounded-lg hover:bg-gray-600 transition-all flex items-center text-white hover:text-blue-300">
                <i class="fas fa-rocket mr-2 text-blue-400"></i>
                Getting Started
            </a>
            <a href="#features" class="p-3 bg-gray-700 rounded-lg hover:bg-gray-600 transition-all flex items-center text-white hover:text-green-300">
                <i class="fas fa-star mr-2 text-green-400"></i>
                Features
            </a>
            <a href="#commands" class="p-3 bg-gray-700 rounded-lg hover:bg-gray-600 transition-all flex items-center text-white hover:text-purple-300">
                <i class="fas fa-terminal mr-2 text-purple-400"></i>
                Commands
            </a>
            <a href="#troubleshooting" class="p-3 bg-gray-700 rounded-lg hover:bg-gray-600 transition-all flex items-center text-white hover:text-yellow-300">
                <i class="fas fa-wrench mr-2 text-yellow-400"></i>
                Troubleshooting
            </a>
            <a href="#support" class="p-3 bg-gray-700 rounded-lg hover:bg-gray-600 transition-all flex items-center text-white hover:text-indigo-300">
                <i class="fas fa-hands-helping mr-2 text-indigo-400"></i>
                Support
            </a>
        </div>
    </div>

    <div class="video-preview mb-12 animate-fade-in">
        <div class="p-1 bg-gradient-to-r from-blue-500 via-purple-500 to-pink-500 rounded-xl">
            <div class="bg-gray-800 rounded-xl overflow-hidden shadow-2xl">
                <div class="aspect-w-16 aspect-h-9">
                    <iframe 
                        src="https://www.youtube.com/embed/_dAKLN2pPjg" 
                        title="<%= locale.docs.video.title %>" 
                        frameborder="0" 
                        allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" 
                        allowfullscreen
                        class="w-full h-full"
                    ></iframe>
                </div>
                <div class="p-6">
                    <h2 class="text-2xl font-bold text-white mb-2 flex items-center">
                        <i class="fas fa-play-circle text-red-500 mr-3"></i>
                        <%= locale.docs.video.title %>
                    </h2>
                    <p class="text-gray-400"><%= locale.docs.video.description %></p>
                </div>
            </div>
        </div>
    </div>

    <section id="overview" class="mb-16">
        <h2 class="text-3xl font-bold mb-8 flex items-center">
            <i class="fas fa-robot text-blue-500 mr-4"></i>
            <%= locale.docs.overview.title %>
        </h2>
        
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
            <% const overviewSections = ['serverManagement', 'protection', 'community', 'configuration']; %>
            <% const overviewIcons = ['server', 'shield-alt', 'users', 'cogs']; %>
            <% const overviewColors = ['blue', 'red', 'green', 'purple']; %>
            
            <% overviewSections.forEach((section, index) => { %>
                <div class="overview-card overflow-hidden transform transition-all duration-300 hover:scale-105" data-aos="fade-up" data-aos-delay="<%= index * 100 %>">
                    <div class="bg-gradient-to-r from-<%= overviewColors[index] %>-600 to-<%= overviewColors[index] %>-700 p-1 rounded-xl">
                        <div class="bg-gray-800 p-6 rounded-xl h-full">
                            <div class="text-<%= overviewColors[index] %>-500 text-3xl mb-4">
                                <i class="fas fa-<%= overviewIcons[index] %>"></i>
                            </div>
                            <h3 class="text-xl font-bold text-white mb-2">
                                <%= locale.docs.overview[section].title %>
                            </h3>
                            <p class="text-gray-400">
                                <%= locale.docs.overview[section].description %>
                            </p>
                        </div>
                    </div>
                </div>
            <% }); %>
        </div>
    </section>

    <section id="features" class="mb-12">
        <h2 class="text-3xl font-bold mb-8 flex items-center">
            <i class="fas fa-star text-yellow-500 mr-4"></i>
            Key Features
        </h2>
        
        <div class="quick-nav grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
            <% const features = ['autoReply', 'tempChannels', 'suggestions', 'protection', 'tickets'] %>
            <% const featureIcons = {
                'autoReply': 'reply',
                'tempChannels': 'volume-up',
                'suggestions': 'lightbulb',
                'protection': 'shield-alt',
                'tickets': 'ticket-alt'
            } %>
            <% const featureColors = {
                'autoReply': 'blue',
                'tempChannels': 'green',
                'suggestions': 'yellow',
                'protection': 'red',
                'tickets': 'purple'
            } %>
            
            <% features.forEach((feature, index) => { %>
                <a href="#<%= feature %>" class="quick-nav-item" data-aos="fade-up" data-aos-delay="<%= index * 100 %>">
                    <div class="bg-gray-800 border-2 border-transparent hover:border-<%= featureColors[feature] %>-500 p-6 rounded-xl hover:bg-gray-700 transition-all transform hover:translate-y-[-5px] shadow-lg">
                        <div class="flex items-center mb-4">
                            <div class="flex-shrink-0 w-12 h-12 flex items-center justify-center bg-<%= featureColors[feature] %>-500 bg-opacity-20 rounded-full mr-4">
                                <i class="fas fa-<%= featureIcons[feature] %> text-<%= featureColors[feature] %>-500 text-xl"></i>
                            </div>
                            <h3 class="text-xl font-bold text-white"><%= locale.docs.features[feature].title %></h3>
                        </div>
                        <p class="text-gray-400"><%= locale.docs.features[feature].description %></p>
                        
                        <div class="mt-4 flex justify-end">
                            <span class="text-<%= featureColors[feature] %>-400 font-medium flex items-center">
                                Learn more <i class="fas fa-chevron-right ml-2"></i>
                            </span>
                        </div>
                    </div>
                </a>
            <% }); %>
        </div>
    </section>

    <div class="docs-content space-y-16">
        <section id="getting-started" class="doc-section">
            <div class="section-header bg-gradient-to-r from-blue-600 to-blue-800 p-6 rounded-t-xl flex items-center">
                <i class="fas fa-rocket text-white text-3xl mr-4"></i>
                <h2 class="text-3xl font-bold text-white"><%= locale.docs.gettingStarted.title %></h2>
            </div>
            <div class="content bg-gray-800 rounded-b-xl p-8 shadow-lg">
                <div class="mb-10">
                    <h3 class="text-2xl font-semibold text-white mb-6 border-b border-gray-700 pb-2">
                        <%= locale.docs.gettingStarted.initialSetup.title %>
                    </h3>
                    <div class="bg-gray-900 p-6 rounded-lg">
                        <ol class="list-decimal list-inside space-y-6 text-gray-300">
                            <% ['invite', 'configure', 'permissions', 'features'].forEach((step, index) => { %>
                                <li class="relative pl-8">
                                    <div class="absolute left-0 top-0 w-6 h-6 rounded-full bg-blue-500 flex items-center justify-center text-white text-sm font-bold">
                                        <%= index + 1 %>
                                    </div>
                                    <span class="text-xl font-semibold text-blue-400"><%= locale.docs.gettingStarted.initialSetup.steps[step].title %></span>
                                    <p class="mt-2 text-gray-400 ml-0"><%= locale.docs.gettingStarted.initialSetup.steps[step].description %></p>
                                </li>
                            <% }); %>
                        </ol>
                    </div>
                </div>

                <div class="requirements-section">
                    <h3 class="text-2xl font-semibold text-white mb-6 border-b border-gray-700 pb-2">
                        <%= locale.docs.gettingStarted.requirements.title %>
                    </h3>
                    <div class="bg-gray-900 p-6 rounded-lg">
                        <ul class="grid grid-cols-1 md:grid-cols-2 gap-4">
                            <% locale.docs.gettingStarted.requirements.items.forEach(item => { %>
                                <li class="flex items-start">
                                    <i class="fas fa-check-circle text-green-500 mt-1 mr-3"></i>
                                    <span class="text-gray-300"><%= item %></span>
                                </li>
                            <% }); %>
                        </ul>
                    </div>
                </div>
            </div>
        </section>

        <% features.forEach((feature, index) => { %>
            <section id="<%= feature %>" class="doc-section" data-aos="fade-up" data-aos-delay="<%= index * 100 %>">
                <div class="section-header bg-gradient-to-r from-<%= featureColors[feature] %>-600 to-<%= featureColors[feature] %>-800 p-6 rounded-t-xl flex items-center">
                    <i class="fas fa-<%= featureIcons[feature] %> text-white text-3xl mr-4"></i>
                    <h2 class="text-3xl font-bold text-white"><%= locale.docs.features[feature].title %></h2>
                </div>
                <div class="content bg-gray-800 rounded-b-xl p-8 shadow-lg">
                    <div class="mb-10">
                        <h3 class="text-2xl font-semibold text-white mb-6 border-b border-gray-700 pb-2">Overview</h3>
                        <p class="text-gray-300 text-lg leading-relaxed"><%= locale.docs.features[feature].description %></p>
                    </div>
                    
                    <div class="features-grid mb-8">
                        <h3 class="text-2xl font-semibold text-white mb-6 border-b border-gray-700 pb-2">Key Capabilities</h3>
                        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                            <% Object.entries(locale.docs.features[feature].features).forEach(([key, value], i) => { %>
                                <div class="feature-card bg-gray-700 hover:bg-gray-600 p-5 rounded-lg transform transition-all hover:scale-[1.02] shadow-md">
                                    <div class="flex items-center">
                                        <div class="w-8 h-8 bg-<%= featureColors[feature] %>-500 bg-opacity-20 rounded-full flex items-center justify-center mr-3">
                                            <i class="fas fa-check text-<%= featureColors[feature] %>-500"></i>
                                        </div>
                                        <h4 class="font-semibold text-white"><%= value %></h4>
                                    </div>
                                </div>
                            <% }); %>
                        </div>
                    </div>
                </div>
            </section>
        <% }); %>

        <section id="commands" class="doc-section" data-aos="fade-up">
            <div class="section-header bg-gradient-to-r from-purple-600 to-purple-800 p-6 rounded-t-xl flex items-center">
                <i class="fas fa-terminal text-white text-3xl mr-4"></i>
                <h2 class="text-3xl font-bold text-white"><%= locale.docs.commands.title %></h2>
            </div>
            <div class="content bg-gray-800 rounded-b-xl p-8 shadow-lg">
                <div class="mb-10">
                    <h3 class="text-2xl font-semibold text-white mb-6 border-b border-gray-700 pb-2 flex items-center">
                        <i class="fas fa-cogs text-blue-400 mr-3"></i>
                        <%= locale.docs.commands.general.title %>
                    </h3>
                    <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                        <% Object.entries(locale.docs.commands.general).filter(([key]) => key !== 'title').forEach(([cmd, data]) => { %>
                            <div class="command-card bg-gray-700 hover:bg-gray-600 p-5 rounded-lg border-l-4 border-blue-500 transition-all shadow-md">
                                <div class="flex items-center mb-2">
                                    <span class="inline-block px-3 py-1 bg-blue-500 bg-opacity-20 text-blue-400 rounded-full font-mono text-sm mr-2">/<%= data.title %></span>
                                </div>
                                <p class="text-gray-300"><%= data.description %></p>
                            </div>
                        <% }); %>
                    </div>
                </div>
                
                <div>
                    <h3 class="text-2xl font-semibold text-white mb-6 border-b border-gray-700 pb-2 flex items-center">
                        <i class="fas fa-shield-alt text-red-400 mr-3"></i>
                        <%= locale.docs.commands.moderation.title %>
                    </h3>
                    <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                        <% Object.entries(locale.docs.commands.moderation).filter(([key]) => key !== 'title').forEach(([cmd, data]) => { %>
                            <div class="command-card bg-gray-700 hover:bg-gray-600 p-5 rounded-lg border-l-4 border-red-500 transition-all shadow-md">
                                <div class="flex items-center mb-2">
                                    <span class="inline-block px-3 py-1 bg-red-500 bg-opacity-20 text-red-400 rounded-full font-mono text-sm mr-2">/<%= data.title %></span>
                                </div>
                                <p class="text-gray-300"><%= data.description %></p>
                                <% if (data.usage) { %>
                                    <div class="mt-3 font-mono text-sm text-gray-500 bg-gray-800 p-2 rounded-md"><%= data.usage %></div>
                                <% } %>
                            </div>
                        <% }); %>
                    </div>
                </div>
            </div>
        </section>

        <section id="troubleshooting" class="doc-section" data-aos="fade-up">
            <div class="section-header bg-gradient-to-r from-yellow-600 to-amber-600 p-6 rounded-t-xl flex items-center">
                <i class="fas fa-wrench text-white text-3xl mr-4"></i>
                <h2 class="text-3xl font-bold text-white"><%= locale.docs.troubleshooting.title %></h2>
            </div>
            <div class="content bg-gray-800 rounded-b-xl p-8 shadow-lg">
                <div class="mb-10">
                    <h3 class="text-2xl font-semibold text-white mb-6 border-b border-gray-700 pb-2 flex items-center">
                        <i class="fas fa-exclamation-triangle text-yellow-500 mr-3"></i>
                        <%= locale.docs.troubleshooting.commonIssues.title %>
                    </h3>
                    <div class="space-y-6">
                        <% ['botNotResponding', 'permissionErrors'].forEach(issue => { %>
                            <div class="issue-card bg-gray-700 p-6 rounded-lg shadow-md">
                                <h4 class="font-semibold text-xl text-yellow-400 mb-3 flex items-center">
                                    <i class="fas fa-times-circle mr-2"></i>
                                    <%= locale.docs.troubleshooting.commonIssues[issue].title %>
                                </h4>
                                <p class="text-gray-300 mb-4"><%= locale.docs.troubleshooting.commonIssues[issue].description %></p>
                                <div class="bg-gray-800 p-4 rounded-lg">
                                    <h5 class="font-medium text-white mb-2">Solution Steps:</h5>
                                    <ul class="space-y-3">
                                        <% locale.docs.troubleshooting.commonIssues[issue].steps.forEach((step, i) => { %>
                                            <li class="flex items-start">
                                                <div class="flex-shrink-0 w-6 h-6 bg-yellow-500 bg-opacity-20 rounded-full flex items-center justify-center mr-3 mt-0.5">
                                                    <span class="text-yellow-500 text-xs font-semibold"><%= i+1 %></span>
                                                </div>
                                                <span class="text-gray-300"><%= step %></span>
                                            </li>
                                        <% }); %>
                                    </ul>
                                </div>
                            </div>
                        <% }); %>
                    </div>
                </div>
            </div>
        </section>

        <section id="support" class="doc-section" data-aos="fade-up">
            <div class="section-header bg-gradient-to-r from-indigo-600 to-indigo-800 p-6 rounded-t-xl flex items-center">
                <i class="fas fa-hands-helping text-white text-3xl mr-4"></i>
                <h2 class="text-3xl font-bold text-white"><%= locale.docs.troubleshooting.support.title %></h2>
            </div>
            <div class="content bg-gray-800 rounded-b-xl p-8 shadow-lg">
                <p class="text-gray-300 text-lg mb-8"><%= locale.docs.troubleshooting.support.description %></p>
                <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                    <a href="https://discord.gg/z82w57MzUC" target="_blank" class="support-card group overflow-hidden relative">
                        <div class="absolute inset-0 bg-gradient-to-r from-indigo-500 to-purple-600 opacity-0 group-hover:opacity-100 transition-opacity duration-300"></div>
                        <div class="bg-gray-700 p-6 rounded-lg relative z-10 h-full transform transition-transform group-hover:scale-[0.98]">
                            <div class="flex items-center">
                                <div class="flex-shrink-0 w-16 h-16 bg-indigo-500 bg-opacity-20 rounded-full flex items-center justify-center mr-4">
                                    <i class="fab fa-discord text-3xl text-indigo-400 group-hover:text-white transition-colors"></i>
                                </div>
                                <div>
                                    <h4 class="font-bold text-xl text-white mb-1"><%= locale.docs.troubleshooting.support.discord.title %></h4>
                                    <p class="text-gray-400 group-hover:text-gray-200 transition-colors"><%= locale.docs.troubleshooting.support.discord.description %></p>
                                </div>
                            </div>
                        </div>
                    </a>
                    <a href="https://github.com/wickstudio" target="_blank" class="support-card group overflow-hidden relative">
                        <div class="absolute inset-0 bg-gradient-to-r from-gray-700 to-gray-900 opacity-0 group-hover:opacity-100 transition-opacity duration-300"></div>
                        <div class="bg-gray-700 p-6 rounded-lg relative z-10 h-full transform transition-transform group-hover:scale-[0.98]">
                            <div class="flex items-center">
                                <div class="flex-shrink-0 w-16 h-16 bg-gray-800 rounded-full flex items-center justify-center mr-4">
                                    <i class="fab fa-github text-3xl text-gray-400 group-hover:text-white transition-colors"></i>
                                </div>
                                <div>
                                    <h4 class="font-bold text-xl text-white mb-1"><%= locale.docs.troubleshooting.support.github.title %></h4>
                                    <p class="text-gray-400 group-hover:text-gray-200 transition-colors"><%= locale.docs.troubleshooting.support.github.description %></p>
                                </div>
                            </div>
                        </div>
                    </a>
                </div>
            </div>
        </section>
    </div>
</div>

<button id="backToTop" class="fixed bottom-8 right-8 bg-blue-600 hover:bg-blue-700 text-white p-4 rounded-full shadow-xl transition-all opacity-0 invisible z-50 transform hover:scale-110">
    <i class="fas fa-arrow-up"></i>
</button>

<style>
    .animate-fade-in {
        animation: fadeIn 0.8s ease-in-out forwards;
    }
    
    .animate-fade-in-delay {
        animation: fadeIn 0.8s ease-in-out 0.3s forwards;
        opacity: 0;
    }
    
    .animate-slide-in {
        animation: slideIn 0.6s ease-out forwards;
    }
    
    @keyframes fadeIn {
        from { opacity: 0; transform: translateY(20px); }
        to { opacity: 1; transform: translateY(0); }
    }
    
    @keyframes slideIn {
        from { opacity: 0; transform: translateX(-20px); }
        to { opacity: 1; transform: translateX(0); }
    }
    
    html {
        scroll-behavior: smooth;
    }
    
    .section-active {
        border-color: #3B82F6;
        background-color: rgba(59, 130, 246, 0.1);
    }
    
    .doc-section {
        transition: transform 0.3s ease-out;
    }
    
    .doc-section:hover {
        transform: translateY(-5px);
    }
</style>

<script>
const backToTop = document.getElementById('backToTop');

window.addEventListener('scroll', () => {
    if (window.scrollY > 300) {
        backToTop.classList.remove('opacity-0', 'invisible');
        backToTop.classList.add('opacity-100', 'visible');
    } else {
        backToTop.classList.add('opacity-0', 'invisible');
        backToTop.classList.remove('opacity-100', 'visible');
    }
    
    const sections = document.querySelectorAll('.doc-section');
    const navLinks = document.querySelectorAll('#toc a');
    
    let currentSectionId = '';
    
    sections.forEach(section => {
        const sectionTop = section.offsetTop;
        if (window.scrollY >= sectionTop - 100) {
            currentSectionId = section.getAttribute('id');
        }
    });
    
    navLinks.forEach(link => {
        link.classList.remove('section-active');
        if (link.getAttribute('href').substring(1) === currentSectionId) {
            link.classList.add('section-active');
        }
    });
});

backToTop.addEventListener('click', () => {
    window.scrollTo({
        top: 0,
        behavior: 'smooth'
    });
});

document.addEventListener('DOMContentLoaded', function() {
    const observerOptions = {
        root: null,
        rootMargin: '0px',
        threshold: 0.1
    };
    
    const observer = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
            if (entry.isIntersecting) {
                entry.target.classList.add('animate-fade-in');
                observer.unobserve(entry.target);
            }
        });
    }, observerOptions);
    
    document.querySelectorAll('.doc-section').forEach(section => {
        section.style.opacity = '0';
        observer.observe(section);
    });
});
</script> 
